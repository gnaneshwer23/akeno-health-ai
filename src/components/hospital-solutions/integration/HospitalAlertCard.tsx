
import React from 'react';
import AlertCard, { AlertCardProps } from '@/components/shared/AlertCard';

const HospitalAlertCard = (props: AlertCardProps) => {
  return <AlertCard {...props} />;
};

export default HospitalAlertCard;
