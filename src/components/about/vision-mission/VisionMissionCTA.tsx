
import React from 'react';
import { motion } from 'framer-motion';

export const VisionMissionCTA = () => {
  // We're removing this component as we've consolidated all CTAs into a single AboutCTA
  return null;
};
